# Completion Checklist

Because the project is not scaffolded yet, completion criteria are currently architectural:

- confirm whether a change belongs in ArchMerOS or in a separate upstream/shared repo
- avoid editing `mero_terminal` without explicit user approval
- after scaffolding, define install, lint, formatting, and verification commands
- keep setup reproducible through version-controlled configs and scripts