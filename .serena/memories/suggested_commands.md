# Suggested Commands

Project is not scaffolded yet. Useful baseline commands:

- `cd /home/zacmero/projects/ArchMerOS`
- `ls -la`
- `rg --files`
- `find . -maxdepth 3 -type f`
- `git init` once repository structure is approved

No project-specific test, lint, format, or run commands exist yet.