#!/bin/sh
exec tail -n +3 $0

# ArchMerOS custom entries are generated by:
#   install/system/apply-bootloader-system.sh
#
# This file is tracked so the intended entry set is visible in the repo, but
# the live /etc/grub.d/40_custom is rendered from the local machine state
# during apply.
